---
description: Viewfly 官方文档（packages/docs）编写规范——受众、API 深度、函数组件用语（组件主体/渲染函数）、提纲与正文分离、正文不收撰稿中间产物、文首不罗列跳转与名词、禁止源码路径、使用教程不写实现细节、禁止写作元话语、禁止用他框架术语定义 Viewfly。
globs: packages/docs/**/*
alwaysApply: false
---

# Viewfly 文档编写规范

约定 **Viewfly 官方文档**（当前为 `packages/docs` 中 VitePress 站点）的受众、结构、写作流程与行文要求。增补或重写文档时遵守本规则；若有冲突，以本规则为准。

## 1. 受众与立场

- **阅读者**：使用 Viewfly 构建应用的开发者。
- **非阅读者**：不以「在本仓库中开发 Viewfly 源码、发 npm 包、跑 monorepo」为叙述对象；不在正文里写克隆仓库、playground、pnpm workspace 等与框架使用无关的流程。
- **作者立场**：文档由 Viewfly 作者对使用者编写，是**对框架能力与用法的权威说明**，语气直接、可执行，避免「我们假设你已经……」式省略。

## 2. API 与能力说明的深度

- 文中出现的 **API**（函数、类型、配置项、包导出等）须具备：**用途**、**最小可运行或最小可理解示例**、**必要参数 / 返回值 / 与周边 API 的关系**、**注意事项与常见踩坑**（边界、顺序、`reflect-metadata` / `JSX` 等交叉点）。
- 避免只列名字或一句话带过；篇幅有限时优先「能跑通的最小场景 + 一两句为何如此」。

## 3. 全站结构：先提纲、后建文件

- 新建或大规模调整目录前：先写 **全站/全册提纲**（章节标题级 + 各块职责与顺序），以 Markdown 放在 **仓库根目录**（如 `DOCS-OUTLINE.md`），**经作者确认或修改后**再在 `packages/docs` 下创建、移动对应文件。
- 未确认提纲前不批量新建空页面。

## 4. 单篇文档：先章节规划、后正文

- 写某一具体 `.md` 正文前：先写该文件的 **章节规划**（二/三级标题 + 每节一句话意图），放在 **仓库根目录** 独立文件（如 `DOCS-page-router.md`），**经作者确认或修改后**再写入 `packages/docs` 对应正文。
- 单篇内遵守「从简单到深入」（见第 7.2 条）。
- **规划稿留在根目录**；勿把与规划文件一一对应的「全文章节树」「阅读顺序树」等**撰稿提纲**粘贴进 **`packages/docs`** 正文（见第 6.4 条）。侧栏「本页目录」由 VitePress 根据标题自动生成。

## 5. 基础与高级两档

### 基础

- 以 **使用案例、端到端小流程** 为主，帮助尽快建立 **整体心智模型**（如安装 → 配置 → 挂载 → 改数据 → 看到更新）；可照抄示例优先于抽象罗列。

### 高级

- 写全 **API 细节**：签名语义、可选与默认、组合方式、与相关 API 的分工；**可适当**说明实现原理或设计意图。
- **禁止**：在文档中直接引用本仓库源码路径或行号，或写「详见 `packages/xxx` 某文件实现」。原理用叙述 + 概念；实现细节以类型定义与行为为准。

## 6. 官方性与时效表述

- **当前文档即正式官方文档**；不写「旧版官网」「仅供参考」「可能过时」「请参阅某外部站点」等把权威让渡给别处的表述。
- 与已发布包行为不一致时，以 **修正文档或发版说明** 解决，不在正文里让读者自行猜版本。

## 6.1 禁止「写作元话语」

- **不向读者**说明「本页/本站参考某外部文档、某框架官网的结构或目录编写」「与 xxx 指南同构」等——那是内部撰稿方法，与使用框架无关。
- 需要对比其他技术时，只写 **Viewfly 的行为与差异**（客观、可验证），不交代「对照谁写的文档」。
- 若维护者需要在仓库根目录提纲（如 `DOCS-OUTLINE.md`）里记录与外部资料的对照，可保留给 **维护与协作**；**`packages/docs` 下读者可见正文**不出现上述元叙述。

## 6.2 只用 Viewfly 语境，避免无关外部框架术语

- **不以其他框架的专属概念定义或衬托 Viewfly**（例如「无单文件组件 / SFC」「无选项式 API」「类似 xxx 的模板语法」）。读者未必熟悉那些框架，这类表述对理解 Viewfly **没有独立信息量**，还容易把文档写成对照表。
- **应写清 Viewfly 自身事实**：例如界面用 **`JSX`/`TSX`** 与**函数组件**、入口 **`createApp`**、响应式 **`reactive` / `signal`**、依赖 **`inject`/`provide`** 等——用本仓库已采用的名称与文件形态说明即可。
- **允许出现的「外部」名词**：通用基础设施与开放标准（如 **Node.js**、**npm**、**Vite**、**TypeScript**、**`JSX` 语法**、**ES 模块**），以及排错时指明应改回的**本框架包名**（如强调 **`jsxImportSource` 必须为 `@viewfly/core`**）；避免用「别家的 API 名」当 Viewfly 章节的标题或定义轴心。

## 6.3 使用教程与实现细节分离

- **指南正文**（尤其 **入门**、**基础** 侧栏下的页面）按**使用教程**写：给出**可复制的写法**、**在页面上 / 在类型上能观察到的效果**、**常见误用与注意点**。
- **禁止**在指南正文里用**内部实现**来教用法：不出现具体私有工具函数名、渲染管线步骤（例如「先经某某函数再 set 到 DOM」）、或未在**公开 API / 公开 `d.ts`** 中承诺的实现细节链条。说明「支持哪些写法」时，用**用户手里的 `JSX` 与类型**，不要用源码里的调用顺序当教案。
- **对外导出的类型名**（如 **`ClassNames`**）可作为 **TypeScript** 约束引用，但不必展开为「内部如何解析」；一句「见类型提示」或示例即可。
- **深入**等偏机制章节若需原理，用**概念级**叙述（调度、依赖、模块生命周期等），仍遵守第 5 条**禁止仓库路径与行号**。

## 6.4 正文不收撰稿中间产物

- **`packages/docs` 下面向读者的 `.md`** 只保留**教程与参考内容**：不出现与 **`DOCS-page-*.md`**、内部提纲**逐节对位**的「全文章节树」「阅读顺序树」等——那是写稿时的**目录草稿**，与学会使用框架无关；重复一遍也会与侧栏「本页目录」冗余。
- **禁止**单独成节的「撰稿说明」式内容：如 **「本页讲什么」「与谁衔接」** 下再列 **「本章：…」「不展开：…」**——与文首引言重复，读起来像作者手记而非用户文档。
- **文首引言**只交代：本页**要教会什么**、**必要前置**（例如须先完成的安装或上一节阅读）。**禁止**在文首用一整段罗列「本页还会出现的 API 名 + 各文档链接」——读者尚未进入语境，容易变成关键词表；与其它章的跳转放在**首次自然写到该能力**时再用一两句带出即可。
- **错误写法后置**：正文主流程优先展示**正确写法**与可运行示例；错误写法统一放在靠后的**注意点 / 排错**小节，且每条都要给出对应改法。避免在开篇或主干段落先铺大量错误示例，影响阅读连贯性。
- **可以保留**的图示：用**用户语言**帮助理解**概念或写法**的短树或列表（例如组件嵌套关系、**`class`** 几种写法的分支），须直接服务当前知识点，而非复制维护者专用规划结构。
- **验收**：发布前检查是否误贴了「本章节目录预览」类块；有则删或改写为概念说明。

## 7. 叙述顺序与密度

### 7.1 函数组件相关用语（统一）

- 指组件**声明函数里、只在创建时执行一次的那段逻辑**（放 **`reactive`**、**`onMounted`** 等）：优先用 **「组件」** 或 **「组件主体」**，按句子自然选用；避免单用「外层」指代。若需与习惯或类型注释对齐，可写「**组件主体**（也常叫 **setup** 阶段）」。
- 指**组件主体返回的、每次刷新界面时会被调用的那个函数**：用 **「渲染函数」** 或 **「组件渲染函数」**，按上下文选用其一即可；避免单用「内层」而不说明职责。若返回的是带 **`render()`** 的对象，则说 **「对象上的 `render`」** 或仍归入「负责画出视图的那一步」。
- **应用模块**的 **`setup(app)`**、**`watch` 的 setup 上下文**等 **API 名里的 `setup`** 保留，不与上一款混淆。

## 7.2 叙述顺序与密度（续）

- **整体顺序**：从简单到深入；同一主题先「能做什么」再「边界与细节」。
- **禁止**：一句话带过关键步骤；默认读者已会某**工具**（如命令行、包管理器）而跳过必要说明时，仍须写清与本框架相关的配置；**禁止**默认读者已会「另一套前端框架」而省略 Viewfly 概念。
- **应写清**：前提、配置项含义、示例与句子的对应关系；必要时用短列表拆步骤。
- **交叉引用**：链到其它页面时，放在读者**已经遇到该问题或该 API** 的上下文中（示例后、注意点、小结一句）；避免在文首或独立小节里「预告式」堆叠链接与名词。

## 附：与工程脚本

- 根目录 `package.json` 的 `docs:dev` / `docs:build` 仅用于本地预览与构建；**不在**文档正文中写「如何参与本仓库开发」类流程。

修订时同步更新本文件。
